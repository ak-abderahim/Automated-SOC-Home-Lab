<!--
Target Path on Server: /var/ossec/etc/ossec.conf on MySOC-Wazuh
Purpose: Instructs the Wazuh manager to look for rule ID 100050 and automatically forward the full event payload as a JSON string to your Shuffle SOAR webhook link.
-->




<ossec_config>
  <global>
    <email_notification>no</email_notification>
    <logall>no</logall>
    <logall_json>no</logall_json>
    <integrity_monitoring_verified_by_head_node>no</integrity_monitoring_verified_by_head_node>
    <alerts_log>yes</alerts_log>
  </global>

  <alerts>
    <log_alert_level>3</log_alert_level>
    <email_alert_level>12</email_alert_level>
  </alerts>

  <remote>
    <connection>secure</connection>
    <port>1514</port>
    <protocol>tcp</protocol>
    <queue_size>131072</queue_size>
  </remote>

  <ruleset>
    <!-- Default ruleset libraries -->
    <decoder_dir>ruleset/decoders</decoder_dir>
    <rule_dir>ruleset/rules</rule_dir>
    <rule_exclude>0215-policy_rules.xml</rule_exclude>
    <list>etc/lists/audit-keys</list>
    <list>etc/lists/amazon/aws-sources</list>
    <!-- User custom ruleset configurations -->
    <decoder_dir>etc/decoders</decoder_dir>
    <rule_dir>etc/rules</rule_dir>
  </ruleset>

  <syscheck>
    <disabled>no</disabled>
    <frequency>43200</frequency>
    <scan_on_start>yes</scan_on_start>
    <directories check_all="yes">/etc,/usr/bin,/usr/sbin</directories>
    <directories check_all="yes">/bin,/sbin,/boot</directories>
    <ignore>/etc/mtab</ignore>
    <ignore>/etc/hosts.deny</ignore>
    <nodiff>/etc/ssl/private</nodiff>
    <skip_nfs>yes</skip_nfs>
  </syscheck>

  <rootcheck>
    <disabled>no</disabled>
    <check_files>yes</check_files>
    <check_trojans>yes</check_trojans>
    <check_dev>yes</check_dev>
    <check_sys>yes</check_sys>
    <check_pids>yes</check_pids>
    <check_ports>yes</check_ports>
    <check_if>yes</check_if>
    <rootkit_files>etc/rootcheck/rootkit_files.txt</rootkit_files>
    <rootkit_trojans>etc/rootcheck/rootkit_trojans.txt</rootkit_trojans>
  </rootcheck>

  <logging>
    <log_format>plain</log_format>
  </logging>

  <!-- Local Manager Internal Telemetry Monitoring -->
  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/auth.log</location>
  </localfile>

  <localfile>
    <log_format>syslog</log_format>
    <location>/var/log/syslog</location>
  </localfile>

  <!-- ============================================================================== -->
  <!-- AUTOMATED SOAR OUTBOUND INTEGRATION ENGINE                                     -->
  <!-- Sends rule alerts directly to Shuffle via webhooks                             -->
  <!-- ============================================================================== -->
  <integration>
    <name>shuffle</name>
    <hook_url>Copy and paste your shuffler.io webhook link here</hook_url>
    <rule_id>100050</rule_id>
    <alert_format>json</alert_format>
  </integration>

</ossec_config>
